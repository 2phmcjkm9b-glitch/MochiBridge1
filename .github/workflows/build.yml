name: Build MochiBridge iOS clean v29

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply v29 Chronos BLE protocol fix
        working-directory: MochiBridge
        run: |
          set -euo pipefail
          python3 fix_v29.py
          grep -F 'TX WITH_RESPONSE' MochiBridge/MochiBridgeApp.swift
          grep -F 'BATTERY REQUEST FROM MOCHI' MochiBridge/MochiBridgeApp.swift
          grep -F 'MOCHI BATTERY RX' MochiBridge/MochiBridgeApp.swift
          grep -F 'MUSIC RX command=' MochiBridge/MochiBridgeApp.swift
          echo 'V29 SOURCE PATCH PASSED'

      - name: Validate source
        working-directory: MochiBridge
        run: |
          set -euo pipefail
          test -f MochiBridge/MochiBridgeApp.swift
          grep -F '6E400001-B5A3-F393-E0A9-E50E24DCCA9E' MochiBridge/MochiBridgeApp.swift
          grep -F '6E400002-B5A3-F393-E0A9-E50E24DCCA9E' MochiBridge/MochiBridgeApp.swift
          grep -F '6E400003-B5A3-F393-E0A9-E50E24DCCA9E' MochiBridge/MochiBridgeApp.swift
          grep -F 'NOTIFY ON — BLE READY FOR TX/RX' MochiBridge/MochiBridgeApp.swift
          grep -F 'AB 00 05 FF 91 80 charging level' MochiBridge/MochiBridgeApp.swift
          grep -F 'TX WITH_RESPONSE' MochiBridge/MochiBridgeApp.swift
          grep -F 'MUSIC RX command=' MochiBridge/MochiBridgeApp.swift
          grep -F 'UIDevice.current.isBatteryMonitoringEnabled = true' MochiBridge/MochiBridgeApp.swift
          echo 'SOURCE VALIDATION PASSED'

      - name: Build unsigned iOS app
        working-directory: MochiBridge
        run: |
          set -euo pipefail
          xcodebuild -project MochiBridge.xcodeproj -scheme MochiBridge -configuration Release -sdk iphoneos -derivedDataPath build CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" | tee ../xcodebuild.log

      - name: Prepare and validate IPA
        run: |
          set -euo pipefail
          APP_PATH=$(find MochiBridge/build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -type d | head -n 1)
          test -n "$APP_PATH"
          rm -rf Payload IPA_CHECK MochiBridge-v29-unsigned.ipa
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          APP_DIR="Payload/$(basename "$APP_PATH")"
          EXECUTABLE="$APP_DIR/MochiBridge"
          test -f "$EXECUTABLE"
          chmod 755 "$EXECUTABLE"
          /usr/libexec/PlistBuddy -c 'Set :CFBundleExecutable MochiBridge' "$APP_DIR/Info.plist" || /usr/libexec/PlistBuddy -c 'Add :CFBundleExecutable string MochiBridge' "$APP_DIR/Info.plist"
          /usr/libexec/PlistBuddy -c 'Set :CFBundleShortVersionString 2.1' "$APP_DIR/Info.plist"
          /usr/libexec/PlistBuddy -c 'Set :CFBundleVersion 29' "$APP_DIR/Info.plist"
          ditto -c -k --sequesterRsrc --keepParent Payload MochiBridge-v29-unsigned.ipa

          mkdir IPA_CHECK
          unzip -q MochiBridge-v29-unsigned.ipa -d IPA_CHECK
          CHECK_APP='IPA_CHECK/Payload/MochiBridge.app'
          CHECK_EXEC="$CHECK_APP/MochiBridge"
          test -f "$CHECK_EXEC"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleExecutable' "$CHECK_APP/Info.plist")" = 'MochiBridge'
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "$CHECK_APP/Info.plist")" = '29'
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$CHECK_APP/Info.plist")" = '2.1'
          test -x "$CHECK_EXEC"
          strings "$CHECK_EXEC" | grep -F 'TX WITH_RESPONSE'
          strings "$CHECK_EXEC" | grep -F 'MOCHI BATTERY RX'
          strings "$CHECK_EXEC" | grep -F 'MUSIC RX command='
          echo 'V29 IPA VALIDATION PASSED'

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: MochiBridge-v29-unsigned-IPA
          path: MochiBridge-v29-unsigned.ipa
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: MochiBridge-v29-build-log
          path: MochiBridge/xcodebuild.log
          if-no-files-found: warn
